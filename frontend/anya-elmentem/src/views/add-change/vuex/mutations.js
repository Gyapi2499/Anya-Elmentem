export default {
  setGroups (state, groups) {
    state.groups = groups
  },
  setMemberList (state, list) {
    state.memberList = list
  },
  setOtherTodos (state, todos) {
    state.otherTodos = todos
  },
  setMyTodos (state, todos) {
    state.myTodos = todos
  }
}
